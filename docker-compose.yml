services:
  service:
    build:
      context: ./
      dockerfile: ./Dockerfile
    volumes:
      - ./config:/config
      - ./logs:/logs
      - ./internal/smtp/templates:/templates
    container_name: service
    image: service
    ports:
      - 9000:9000
